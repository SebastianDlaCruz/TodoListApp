import styled from 'styled-components';

export const SectionAuthStyle = styled.section`
    position: relative;
    block-size: 100vh;
    background-color: ${({ theme }) => theme.dark};
    color: #fff;
`;